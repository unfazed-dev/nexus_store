name: nexus_store_drift_adapter
description: Drift adapter for nexus_store for local-only SQLite storage.
version: 0.1.0
repository: https://github.com/user/nexus_store
publish_to: none

environment:
  sdk: ^3.5.0

resolution: workspace

dependencies:
  nexus_store:
    path: ../nexus_store
  drift: ^2.22.0
  rxdart: ^0.28.0

dev_dependencies:
  drift_dev: ^2.22.0
  build_runner: ^2.4.0
  test: ^1.25.0
  mocktail: ^1.0.4
  lints: ^5.1.0
  sqlite3: ^2.4.0
