name: nexus_store_riverpod_generator
description: Code generator for nexus_store_riverpod_binding. Generates Riverpod providers from @riverpodNexusStore annotations.
version: 0.1.0
repository: https://github.com/unfazed-dev/nexus_store

environment:
  sdk: ^3.5.0

dependencies:
  build: ^2.4.0
  source_gen: ^1.5.0
  analyzer: ^6.0.0
  nexus_store_riverpod_binding: ^0.1.0
  meta: ^1.16.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.8
  build_test: ^2.2.2
  test_reporter:
    git:
      url: https://github.com/unfazed-dev/test_reporter.git
      ref: main
