name: nexus_store_entity_generator
description: Code generator for nexus_store type-safe entity field accessors.
version: 0.1.0
repository: https://github.com/unfazed-dev/nexus_store

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  analyzer: ^6.0.0
  build: ^2.4.0
  nexus_store: ^0.1.0
  source_gen: ^1.5.0

dev_dependencies:
  build_runner: ^2.4.0
  build_test: ^2.2.0
  test: ^1.24.0
  test_reporter:
    git:
      url: https://github.com/unfazed-dev/test_reporter.git
      ref: main
