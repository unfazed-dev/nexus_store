name: nexus_store_brick_adapter
description: Brick adapter for nexus_store with offline-first capabilities.
version: 0.1.0
repository: https://github.com/user/nexus_store

environment:
  sdk: ^3.5.0

resolution: workspace

dependencies:
  nexus_store:
    path: ../nexus_store
  # brick_offline_first_with_supabase: ^2.1.0

dev_dependencies:
  test: ^1.25.0
  mocktail: ^1.0.4
  lints: ^5.1.0
