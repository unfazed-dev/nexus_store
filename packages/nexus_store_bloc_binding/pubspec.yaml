name: nexus_store_bloc_binding
description: Bloc/Cubit bindings for NexusStore - provides familiar Bloc patterns with NexusStore data.
version: 0.1.0
repository: https://github.com/unfazed-dev/nexus_store

environment:
  sdk: ^3.5.0

dependencies:
  bloc: ^8.1.4
  nexus_store: ^0.1.0
  meta: ^1.12.0

  collection: any
dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
  mocktail: ^1.0.4
  bloc_test: ^9.1.7
  test_reporter:
    git:
      url: https://github.com/unfazed-dev/test_reporter.git
      ref: main
