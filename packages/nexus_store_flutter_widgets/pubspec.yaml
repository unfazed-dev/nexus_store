name: nexus_store_flutter_widgets
description: Flutter widgets for nexus_store with StreamBuilder, providers, and reactive builders.
version: 0.1.0
repository: https://github.com/unfazed-dev/nexus_store

environment:
  sdk: ^3.5.0
  flutter: ">=3.22.0"


dependencies:
  flutter:
    sdk: flutter
  nexus_store: ^0.1.0
  rxdart: ^0.28.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mocktail: ^1.0.4
  test_reporter:
    git:
      url: https://github.com/unfazed-dev/test_reporter.git
      ref: main
